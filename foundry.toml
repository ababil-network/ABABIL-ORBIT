[profile.default]
src = "contracts"
test = "tests"
script = "script"
out = "out"
cache_path = "cache"

solc_version = "0.8.36"

optimizer = true
optimizer_runs = 200

[fmt]
line_length = 100
tab_width = 4
bracket_spacing = true
